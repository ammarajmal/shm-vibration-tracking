# Core scientific stack
numpy>=1.20
scipy>=1.6
pandas>=1.3

# Vision / image IO
opencv-python>=4.5

# AprilTag detection
pupil-apriltags>=1.0.4

# Config + CLI + progress
PyYAML>=6.0
tqdm>=4.60

# Plotting (for diagnostics + paper figures)
matplotlib>=3.3

# Optional: nicer CLI logging / formatting
rich>=13.0

# Optional: if you export parquet
pyarrow>=10.0

# Optional: testing
pytest>=7.0
pycryptodomex>=3.10
python-gnupg>=0.5.0
rospkg>=1.3.0
